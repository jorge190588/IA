
--------------------CUESTIONARIO DEL VIDEO #2----------------------

1. ¿Qué es TensorFlow? 
.(Ok) Una biblioteca de código abierto desarrollada por google que se basa en un sistema de redes neuronales.
.(x) Un lenguaje de programación como cualquier otro
.(x) Lenguaje que aprende y enseña
.(x) Lenguaje que enseña a programar

2. ¿Cuál es la forma estandar de importar TensorFlow?
.(Ok) import tensorflow as tf
.(x)  import tensorflow as tensorflow
.(x)  import as tf
.(x)  import tf as

3. ¿Cuáles son ejemplos de imports en tensorflow?
.(Ok) import loadmat, import plotly.offline a py, import plotly.graph_objs as go, import numpy as np, import random, etc.
.(x)  import tensorflow as tensorflow, import go, import as, etc.
.(x)  import ten, import import, import sum, etc.
.(x)  import atf, import square rum, import calculo, etc.

4. ¿Cuál es la forma de definir variables en tensorflow? 
.(x)  int x
.(x)  float none y
.(ok) x = tf.placeholder("float", [None, 784])
.(x)  char w


5. ¿Para que sirve la funcion Softmax?
.(ok)  Para las categorías mutuamente excluyentes y asi mejorar el aprendizaje.
.(x)   Para las categorías diversas
.(x)   Para calcular una salida
.(x)   Para especificar salidas

6. ¿Cuál sería la linea de código para generar una prediccion?
.(ok)  prediction = prediccion.prediccion(x[n:n+1])
.(x)   prediction = prediccion.predict(x[n:n+1])
.(x)   prediccion = modelo.predictivo(x[n:n+1])
.(ok)  prediction = model.predict(x[n:n+1])

7. ¿Cuál sería la línea de código para generar una prediccion con el argumento máximo, sin tomar en cuenta la impresión de resultados?
.(x)  prediction = prediccion.prediccion(x[n:n+1])
.(ok) prediction = np.argmax(prediction)
.(x)  prediccion = modelo.predictivo(maximo)
.(x)  prediction = np.argumentomaximo(salida)

8. ¿Que es MNIST?
  .(ok) esta formado por imagenes en blanco y negro digitos hechos a mano.
  .(x) permite obtener la probabilidad de cada clase
  .(x) incluye reconocimiento de patrones y se empieza a crear un modelo.
  
 9. ¿Que es Deep Learning?
   .(x) esta formado por imagenes en blanco y negro digitos hechos a mano.
  .(x) permite obtener la probabilidad de cada clase
  .(ok) incluye reconocimiento de patrones y se empieza a crear un modelo de reconocimiento de numeros escritos a mano
  
  10. ¿Para que sirve la herramienta Potly?
    .(x) para crear grafos
    .(ok) para graficar las imagenes
    .(x) creacion de matrices
    
  11. ¿Que significa docker?
    .(ok) es un proyecto de codigo abierto con el que facilmente podremos crear "contenedores".
    .(x) permite obtener la probabilidad de cada clase
    .(x) Una biblioteca de código abierto desarrollada por google que se basa en un sistema de redes neuronales.
    
   12. ¿Que es phyton?
    .(x) Lenguaje que aprende y enseña
    .(ok) Es un lenguaje interpretado, usa tipado dinámico y es multiplataforma.
    .(x) Para las categorías mutuamente excluyentes y asi mejorar el aprendizaje.
    
    
